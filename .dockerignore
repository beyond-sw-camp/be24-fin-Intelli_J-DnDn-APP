# 의존성 — 컨테이너 안에서 pnpm install로 새로 설치
node_modules

# 네이티브 빌드 결과물 — 웹 파드 배포에 불필요
android
ios

# 이전 빌드 결과물 — 컨테이너 안에서 vite build로 새로 생성
dist

# 개발 환경 파일
.env.local
.env.*.local

# 에디터 / OS
.DS_Store
.vscode
*.log
